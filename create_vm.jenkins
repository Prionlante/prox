#!groovy
properties([disableConcurrentBuilds()])

pipeline {
 agent {
    label 'prox'
  }
  triggers { pollSCM('* * * * *') }
  options{
    timestamps()
  }
  stages {
    stage('Create virtual machine') {
      steps {
        sh 'echo \'hi, lol\''
      }
    }
  }
}
