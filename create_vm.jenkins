#!groovy
properties([disableConcurrentBuilds()])

pipeline {
 agent {
    label 'Jenkins'
  }
  options{
    timestamps()
  }
  stages {
    stage('Create virtual machine') {
      steps {
        sh 'pwd'
      }
    }
  }
}
