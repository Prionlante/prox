#!groovy
properties([disableConcurrentBuilds()])

pipeline {
 agent {
    label 'prox'
  }
  options{
    timestamps()
  }
  stages {
    stage('Create virtual machine') {
      steps {
        sh 'pwd; ls'
      }
    }
  }
}
