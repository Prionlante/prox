#!groovy
properties([disableConcurrentBuilds()])

pipeline {
  agent {
    label 'scrpt'
  }
  options{
    timestamps()
  }
  stages {
    stage('Create virtual machine') {
      steps {
        sh 'pwd'
      }
    }
  }
}
