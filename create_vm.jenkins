#!groovy
properties([disableConcurrentBuilds()])

pipeline {
 agent {
    label 'мастер'
  }
  options{
    timestamps()
  }
  stages {
    stage('Create virtual machine') {
      steps {
        sh 'pwd'
      }
    }
  }
}
