#!groovy
properties([disableConcurrentBuilds()])

pipeline {
 agent {
    label 'Мaster'
  }
  options{
    timestamps()
  }
  stages {
    stage('Create virtual machine') {
      steps {
        sh 'pwd'
      }
    }
  }
}
